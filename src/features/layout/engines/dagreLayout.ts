import { AppNode, AppEdge, LayoutType } from '../../../store/types';

export function getDagreLayout(
  nodes: AppNode[],
  _edges: AppEdge[],
  _direction: LayoutType,
): AppNode[] {
  // Dagre automatic node positioning calculation stub
  return nodes;
}
